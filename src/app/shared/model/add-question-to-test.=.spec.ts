import { AddQuestionToTest } from './add-question-to-test.=';

describe('AddQuestionToTest', () => {
  it('should create an instance', () => {
    expect(new AddQuestionToTest()).toBeTruthy();
  });
});
